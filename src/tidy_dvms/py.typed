# from repo root
python -m venv .venv
# Windows:
.venv\Scripts\activate
# macOS/Linux:
# source .venv/bin/activate

pip install -U pip
pip install -e .[dev]  # if you added optional dev deps; else just: pip install -e .

python -c "from dvms_client import DVMS; print(DVMS)"
pytest -q
